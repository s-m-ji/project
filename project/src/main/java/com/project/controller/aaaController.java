package com.project.controller;

import javax.servlet.http.HttpServletRequest;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

import com.project.service.MemberService;

@Controller
public class aaaController {
	MemberService memberservice;
	
	/*
	 * @GetMapping("/login/naver") public void naverLogin() {
	 * 
	 * }
	 * 
	 * @GetMapping("/login/naver_callback") public String
	 * naverLogin_callback(HttpServletRequest request , Model model) {
	 * System.out.println("하이!"); memberservice.naverLogin(request, model);
	 * 
	 * return "/recipe/list";
	 * 
	 * }
	 */
	
}
